STEP A:
git config --global user.name "Zehra Mey Öztürk"
git config --global user.email "23comp1005@isik.edu.tr"
git config --global core.editor "nano"

Verification:
credential.helper=osxkeychain
init.defaultbranch=main
user.email=23comp1005@isik.edu.tr
user.name=Zehra Mey Öztürk
filter.lfs.clean=git-lfs clean -- %f
filter.lfs.smudge=git-lfs smudge -- %f
filter.lfs.process=git-lfs filter-process
filter.lfs.required=true
core.editor=nano


STEP B:
Commands used:
cd Desktop
mkdir softhw
cd softhw
git clone https://github.com/peccatumx/soft2101-homework4.git
ls
cd soft2101-homework4
ls -a

Output:
soft2101-homework4
.
..
.git
README.md
hwk4

Answer to question:
Command used:
ls .git

Output:
HEAD
config
description
hooks
info
index
logs
objects
packed-refs
refs

Explanation:
The local .git directory contains Git’s internal files and folders that store
repository configuration, commit history, objects, references, and logs.


STEP C:
README.md was edited to describe the repository and its contents.

Markdown link answer:
[Department of Software Engineering - Işık University](https://www.isikun.edu.tr/akademik/muhendislik-fakultesi/bolumler-ve-programlar/bilgisayar-muhendisligi/programlar/lisans-programi/yazilim-muhendisligi)


STEP D:
Commands:
git add .gitignore
git commit -m "setting up gitignore"

Output:
[main 6220b03] setting up gitignore
 1 file changed, 21 insertions(+)
 create mode 100644 .gitignore

QUESTION: What rules are in your .gitignore?

Answer:
.DS_Store
*.class
*.log
*.jar
.idea/
.vscode/
*.iml
target/
bin/
out/
*~


STEP E:
Commands:
git branch feature-1
git checkout feature-1
cd hwk4

Output:
* feature-1
  main
/Users/mey/Desktop/softhw/soft2101-homework4/hwk4
homework4.txt


STEP F:
Commands:
git checkout feature-1
mkdir -p hmk1
cd hmk1
nano Main.java
javac Main.java
java Main

Program Output:
3 8
4
5
6
7

Commit:
git add hmk1/Main.java
git commit -m "satisfying requirement one"
git push origin feature-1

Explanation:
A new Java program was created in a separate branch (feature-1) to satisfy the first project requirement.
The program was successfully compiled and executed, and its output was verified.
The change was committed and pushed to the remote feature branch.

QUESTION:
What are all the commits you’ve made on the feature-1 branch?

Answer:
fabb6c6 satisfying requirement one
e9e05a3 Add STEP F solution
c2ec8d8 Add STEP F question answers

What files (and sub-directories, if any) are listed in your hmk1 directory on the feature-1 branch?

Answer:
Main.java

What files (and sub-directories, if any) are listed in your hmk1 directory on the main branch?

Answer:
None. The hmk1 directory does not exist on the main branch.
